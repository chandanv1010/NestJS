import { Injectable } from "@nestjs/common";
import { Logger } from "@nestjs/common";


@Injectable()
export class MailService {
    private readonly userLogger = new Logger(MailService.name)


    constructor(
       
    ){
       
    }


}